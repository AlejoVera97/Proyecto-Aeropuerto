@model  EntidadesCompartidas.Aeropuertos

@{
    ViewBag.Title = "LISTAR AEROPUERTOS";
}

<h2>LISTADO DE AEROPUERTOS</h2>

@ViewBag.Mensaje


<p>

    @Html.ActionLink(" Volver", "Principal", "Home")
</p>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <div>
            @Html.TextBox("DatoFiltro")
            <input type="submit" value="Filtrar" class="btn btn-default" />
        </div>
    </div>
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDAeropuerto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direccion)
        </th>
        <th></th>
    </tr>

</table>
 @foreach (EntidadesCompartidas.Aeropuertos aeropuertos in Model.IDAeropuerto)
 {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => aeropuertos.IDAeropuerto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => aeropuertos.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => aeropuertos.Direccion)
            </td>
            <td>
                @Html.ActionLink("Modificar", "FormArticuloModificar", new { aeropuertos.IDAeropuerto }) @**@
@Html.ActionLink("Consultar", "FormArticuloConsultar", new { aeropuertos.IDAeropuerto })
@Html.ActionLink("Borrar", "FormArticuloEliminar", new { aeropuertos.IDAeropuerto })
            </td>
        </tr>
 }




